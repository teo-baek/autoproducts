# 📋 AutoProducts 확정 기획서 (Planned Features)

> 이 문서는 사용자와의 논의를 통해 개발이 '확정'된 기획 내용만 영구적으로 기록하는 문서입니다. 코딩은 이 문서에 명시된 비즈니스 룰을 최우선으로 따릅니다.

## 1. 팝오버(Pop-over) 간편 예약 및 뷰 분리
- **상태:** ✅ 개발 완료
- **내용:** 소매상의 인지 과부하를 막기 위해 상품 카탈로그 화면과 예약 관리 화면을 분리. 예약은 상품 카드 내에서 팝오버를 띄워 화면 이동 없이 처리.
- **UI 원칙:** 이미지 비율 유지(Fit) 및 모바일 최우선 유동형(Auto-Height) 레이아웃 적용.

## 2. 실시간 통계 및 AI 비즈니스 인사이트
- **상태:** ✅ 개발 완료
- **내용:** 도매상에게 상품 조회(클릭) 수, 전환율(STR), 인기 상품 통계 제공.
- **비용 최적화 룰:** 통계 화면 최상단에 AI(Gemini 2.5)가 3줄 요약 브리핑을 제공하되, 토큰 비용 방어를 위해 **'일 1회 세션 캐싱(Caching)'** 적용. 데이터가 없는 경우(DB 스키마 누락 등) 에러를 뿜지 않고 기본값으로 렌더링.

## 3. AI 도매 매니저 분리 및 하이브리드 스마트 검색
- **상태:** 📝 코딩 대기 중 (확정)
- **챗봇 메뉴 분리:** 소매상의 카탈로그 하단에 있던 챗봇을 좌측 사이드바 `[💬 AI 도매 매니저 상담]` 독립 메뉴로 분리.
- **프리미엄 요금제 제어:** 챗봇 대화 기능은 해당 도매상이 `has_ai_agent(프리미엄)` 권한이 있을 때만 동작. (없을 시 자물쇠 블라인드 처리)
- **카탈로그 스마트 검색 (Auto-Switching):** 상단 검색바에서 검색어 맥락(길이, 띄어쓰기, 종결어미 등)을 파악하여 일반 검색과 AI 의미론적 검색을 자동 분기함.
- **스마트 검색 과금 정책:** 카탈로그 스마트 검색 기능은 도매상의 **구독 플랜 등급과 무관하게 모든 유저에게 무료로 제공.** (추후 토큰 비용 절감을 위한 R&D 별도 진행)

## 4. 상품 상세설명(Description) 및 비고란 추가
- **상태:** 📝 코딩 대기 중 (확정)
- **DB 확장:** `products` 테이블에 `description (TEXT)` 컬럼 추가.
- **엑셀 연동:** 도매상이 업로드하는 엑셀의 "상세설명(또는 비고)" 열을 파싱하여 DB에 저장.
- **UI 노출 금지 (Progressive Disclosure):** Streamlit UI의 깔끔함을 유지하기 위해 카탈로그 상품 카드에는 해당 설명을 텍스트로 노출시키지 않음. (추후 디자인 전체 개편 시 재검토)
- **AI 챗봇 지식(Knowledge) 활용:** 파싱된 `description` 데이터를 소매상용 AI 챗봇의 프롬프트 컨텍스트에 주입. (재고 부족 시 주문 제작 대응 등 영업에 활용)
- **🚀 퓨처 비전 (Future R&D):** 향후 도매상이 채팅창에 이미지와 설명을 던지면 AI가 알아서 스키마(원단, 핏, 제작기간 등)에 맞게 정형화하여 DB 컬럼값을 채우는 'AI Data Entry Agent' 시스템으로 고도화 예정.
