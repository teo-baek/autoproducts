# 🧪 AUTOPRODUCTS 실행 및 E2E 테스트 가이드

이 문서는 개발자 및 QA 담당자가 로컬 환경에서 **AUTOPRODUCTS (도매상 앱 ↔ 소매상 웹 실시간 동기화 플랫폼)** 전체 시스템을 구동하고 핵심 시나리오를 테스트하기 위한 완벽 가이드입니다.

---

## 🛠️ 1. 사전 준비 (Prerequisites)

시스템을 구동하기 위해 아래 프로그램들이 PC에 설치되어 있어야 합니다.
*   **Node.js:** v22 이상 (LTS 권장)
*   **Docker Desktop:** (Supabase 로컬 DB를 띄우기 위해 필수 실행 상태여야 합니다)
*   **Git:** 소스코드 관리를 위해 필요
*   **스마트폰 또는 에뮬레이터:** Expo 모바일 앱 테스트용 (iOS 시뮬레이터 또는 Android 스튜디오 에뮬레이터, 혹은 실물 기기에 'Expo Go' 앱 설치)

---

## 🚀 2. 전체 시스템 구동 방법

터미널을 열고 프로젝트 루트 폴더(`autoproducts`)에서 아래 순서대로 실행하세요.

### Step 2-1: 패키지 의존성 설치
```bash
# 각 폴더별로 패키지를 설치합니다.
npm install
cd apps/web && npm install
cd ../mobile && npm install
cd ../..
```

### Step 2-2: 로컬 데이터베이스(Supabase) 실행
Docker Desktop이 켜져 있는지 확인한 후 아래 명령어를 입력합니다.
```bash
npx supabase start
```
*   *(주의: 최초 실행 시 Docker 이미지를 다운로드하느라 수 분이 소요될 수 있습니다.)*
*   실행 완료 후 터미널에 나오는 `API URL`과 `anon key`는 자동으로 앱/웹의 기본 환경변수로 연결되도록 세팅되어 있습니다.

### Step 2-3: Next.js 소매상 웹(Web) 서버 실행
새로운 터미널 탭을 열고 아래 명령어를 실행합니다.
```bash
cd apps/web
npm run dev
```
*   웹 브라우저를 열고 **`http://localhost:3000`** 에 접속합니다.

### Step 2-4: Expo 도매상 모바일 앱(Mobile) 서버 실행
또 다른 새로운 터미널 탭을 열고 아래 명령어를 실행합니다.
```bash
cd apps/mobile
npx expo start
```
*   터미널에 나타난 QR 코드를 스마트폰의 카메라(또는 Expo Go 앱)로 스캔하거나, 터미널에서 `i` (iOS) 또는 `a` (Android)를 눌러 가상 에뮬레이터를 띄웁니다.

---

## 🎬 3. E2E(End-to-End) 통합 시나리오 테스트 방법

서버가 모두 켜졌다면, **"도매상이 등록한 상품을 소매상이 라이브 방송에서 판매하는 시나리오"**를 순서대로 테스트합니다.

### 🔴 Scene 1: 도매상의 초고속 상품 등록 (앱)
1.  에뮬레이터(또는 스마트폰)에 띄워진 **모바일 앱**을 봅니다.
2.  하단 탭에서 **[초고속 스피드 등록]** 아이콘을 누릅니다.
3.  화면 중앙의 `[+ 정면]` 빈 슬롯을 터치하여 카메라 또는 갤러리에서 사진을 업로드합니다.
4.  아래쪽 **도매 단가 입력** 칸에 `35000`과 같은 숫자를 입력합니다.
5.  **[💾 저장 및 QR 프린트]** 버튼을 누릅니다.
    *   *결과 검증:* "DB 저장 및 라벨 인쇄가 완료되었습니다" 알림이 뜨며 입력 폼이 빈 상태로 초기화되어야 합니다.

### 🟢 Scene 2: 소매상의 글로벌 카탈로그 확인 (웹)
1.  PC 웹 브라우저(`http://localhost:3000`)를 봅니다.
2.  홈 화면 우측 하단의 **권한 스위치**를 눌러 **[🛒 소매상 모드]**로 변경합니다.
3.  메인 화면에서 **[📖 글로벌 카탈로그]** 버튼을 클릭하여 진입합니다.
4.  *결과 검증:* 방금 모바일 앱(Scene 1)에서 등록했던 옷 사진과 가격이 미니멀한 화보(Vogue) 스타일의 리스트로 실시간 노출되는지 확인합니다. (마우스를 올리면 가격이 뜹니다.)

### 🔵 Scene 3: 라이브 방송 실시간 재고 차감 (웹)
1.  웹 브라우저에서 뒤로가기를 누른 뒤, **[🔴 라이브 방송 대시보드]** 버튼을 클릭해 진입합니다.
2.  도매상이 할당해 준 '잔여 가상재고(기본 50개)' 목록이 보입니다.
3.  우측 상단의 **[방송 시작 (ON AIR)]** 버튼을 눌러 방송 모드를 켭니다.
4.  상품 리스트 옆에 있는 파란색 **[-1] 버튼**을 마우스로 연타해 봅니다.
    *   *결과 검증:* 지연 현상(렉) 없이 잔여 재고 숫자가 즉각 줄어들고 판매량이 올라가야 합니다. (Supabase RPC 및 Row-level Lock 덕분에 동시 클릭 시에도 에러가 나지 않습니다.)
5.  테스트가 끝나면 화면 하단의 **[📋 도매상에게 발주서 전송]** 버튼을 클릭합니다.
    *   *결과 검증:* 총 판매된 개수만큼 도매상에게 발주서가 전송되었다는 알림이 뜨고 방송 모드가 종료됩니다.

---

## 🛠️ 4. 트러블슈팅 (FAQ)

**Q. Supabase start 실행 시 에러가 납니다.**
A. Docker Desktop 프로그램이 실행되지 않아서 발생하는 문제입니다. 윈도우/맥에서 Docker Desktop 앱을 켜고 고래 아이콘이 녹색(Running)이 된 후 다시 시도하세요.

**Q. 웹이나 앱에서 흰 화면이 나오거나 데이터를 못 불러옵니다.**
A. `npx supabase status`를 입력하여 로컬 DB 컨테이너들이 모두 정상 작동 중(healthy)인지 확인하세요. 또한 `apps/web/src/lib/supabase.ts` 파일의 접속 주소가 현재 환경(localhost 등)과 맞는지 체크해 주세요.
